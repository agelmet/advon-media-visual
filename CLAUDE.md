@AGENTS.md

# advonmedia.com — project notes (Sept 2026)

## Stack (verified from the repo)
- Next.js 16 (app router, JSX), React 19, Tailwind 4 via @tailwindcss/postcss, zustand for the EL/EN language store, lucide-react icons.
- Live site is served by **Netlify** (not the Vercel project `advon-media-visual`, which is a mirror). Env vars go in Netlify.
- `public/crm/index.html` is the Advon CRM (single-file, encrypted, GitHub-backed sync via `app/api/crm/route.js`). Always back it up as `index.backup-YYYYMMDD.html` before editing; keep single-file, encrypted storage and sync layer intact.
- The Advon chat widget is an external script from advon-services.vercel.app loaded in `app/layout.jsx`.

## Audit of 5 Sept 2026 (PageSpeed, mobile, Slow 4G) — the numbers to beat
Performance 48 · Accessibility 82 · Best practices 77 · SEO 100 · FCP 3.2s · LCP 4.8s · TBT 1,320ms · CLS 0 · Speed Index 5.2s · 2,766 KiB · unused JS 927 KiB · render-blocking 1,520ms · 32 non-composited animations · no CrUX field data yet.
Root causes: Google Fonts via CSS @import; Playfair Display has no Greek glyphs (Greek headlines render in the system fallback serif); whole homepage is a client component pulling reviews.js/services.js/data.js into the bundle; starfield + aurora blur + laser sweeps + scanlines + custom cursor; blur() scroll reveals that leave the viewport blank; widget + Zoho iframe + raw.githubusercontent logo at load; no CSP/HSTS/XFO headers; a11y issues (form labels, iframe title, contrast, heading order, prohibited ARIA).

## Rebuild rules (direction chosen by Angelo: dark, cinematic)
- Server-render pages; hydrate only the language toggle, FAQ accordion and reviews strip. Ship the first 12 reviews, load the rest on demand.
- Fonts via `next/font` with `subsets: ['greek','latin']`: display GFS Didot (fallback Literata), body Manrope, figures JetBrains Mono. Never a face without the greek subset.
- Remove starfield, aurora blobs, lasers, scanlines, custom cursor and every looping glow. One accent (the Advon logo blue); gold only for stars; lime and purple retired. Warm-black ground (#0B0E12 → #141A21), warm off-white text (#F2EEE6), a static 4% grain layer.
- Hero: headline + ONE CTA + a slow cross-fading stack of real site mockups (assets in ADVON-CLIENTS/portfolio-heroes). Proof line with the Google logo linking to the live profile.
- Section order: hero → niche strip → «Πώς δουλεύει» (3 steps) → portfolio with niche filter → pricing card → reviews → three service tiles + link → FAQ → booking → footer.
- Reveals ≤ 500ms, transform/opacity only, rootMargin -8%; nothing blank at any scroll position.
- Self-host the logo; load the chat widget on first interaction; open the Zoho embed on click; sticky mobile bar Κλήση · Viber · Ραντεβού.
- Add `/dorean-istoselida` (ads landing page: promise text verbatim from the October Blueprint, three-site strip, review wall, 3-field form posting to the forms rail + CRM lead endpoint with utm_source/campaign/content) and its thank-you page with the Zoho embed; Meta Pixel Lead on submit, Schedule on booking.
- Security headers (CSP, HSTS, XFO, COOP) in netlify.toml; add llms.txt; OG image per page is a real mockup.
- Budget before any push: mobile Lighthouse ≥ 90 performance / ≥ 95 accessibility, LCP ≤ 2.0s, ≤ 900KB. Keep the review count consistent with the live Google figure everywhere (site, schema, ads).

## Working notes (5 Sept 2026, after the v2 rebuild)
- Repo: `agelmet/advon-media-visual` (public); Netlify project `advonmediaa` (team `advonmd`) deploys every push in ~2 min; Vercel and GitHub Pages also build it but are mirrors only.
- **Angelo's decision, 6 Sept 2026: the current design stays.** He likes the starfield site and the 2.x CRM as they are. Work on `main` is *optimisation only* — never change copy, structure, colours or the effects he sees; you may add premium touches and make things faster. The v2 rebuild stays parked on `v2-draft` (see below); the funnel/landing page work resumes together with him before the October ads.
- **What the 6 Sept optimisation did on `main`** (commit `aca7797`): fonts via `next/font` (`lib/fonts.js`, Playfair Display + Inter — same faces), self-hosted logo/icons in `public/img/`, `lib/nav.js` for Header/Footer menu labels (keeps `lib/services.js` out of the shared JS), reviews cards + list in a lazy chunk (`components/ReviewsColumns.jsx`, mounted ~700px before view), Zoho embed injected when the contact section is ~900px away, chat widget on first interaction (`DeferredWidget.jsx`), starfield redrawn with sprites + batched fills + 30fps on touch + paused when hidden, all `filter: blur()` aurora blobs replaced by wide radial gradients (same look), intro keyframes are opacity/transform only, security headers + `llms.txt` from `next.config.ts`. Premium additions: film grain (`.grain`), headline light sweep (`.hero-sheen`), pointer spotlight on glass cards (`Spotlight.jsx`), shooting stars, reading-progress hairline (`ScrollProgress.jsx`). Local mobile Lighthouse 48 → 91 perf, 82 → 96 a11y, 77 → 100 best practices.
- **CRM 2.x on `main` (6 Sept, commit `b8e93a4`):** Home has a «Do ASAP» numbered list at the top (`state.asap`, 🔥 = red super-high priority, drag ⋮⋮ to reorder, Clear done), the subscriptions card moved to the bottom, and the call statistics were restarted: on first load after this version the old Quick Log calls are archived into `state.meta.callLogArchive` (Export menu → «Restart call statistics…» / «Restore archived calls»), so every call number counts from the first call logged after the restart (mid/late September). Clients, renewals, subscriptions and money untouched. Backup: `public/crm/index.backup-20260906.html`.
- **CRM 2.x on `main` (7 Sept, two commits):** (a) `53e509e` — one-time v2 wipe of every cold-call statistic (`state.meta.callStatsMigrated='2026-09-07-v2'`) and extra services per client (`c.extras`, `cval()` feeds every money figure). (b) new «Remaining» tab (before Year): every unpaid client grouped by month with its stage, days waiting and value; totals banner; per-client colour flags `c.remFlag` = green «About to pay» / orange «Most likely lost» / red «About to trash»; «Paid» button collects; bin button sets `c.trashed` (+`trashedAt`, `trashReason`, `trashStage`) — trashed clients leave every board/target/revenue figure (`notLost` now also excludes them) and live only in Remaining → Trash as lost money, with Restore. Call statistics also count from a start date, `state.meta.statsSince` (default `2026-09-15`, `STATS_SINCE_DEFAULT`): Quick Log / Year / Home call numbers, Meeting → client and revenue-in-stats ignore anything before it (older log days show «not counted»); Export → «Restart call statistics…» asks for the date. Home hero grouped into Calls / Money / Attention (same labels); global CSS polish block at the end of the styles. Backup: `public/crm/index.backup-20260907.html`.
- **Renewals redesign (7 Sept, same day):** the Renewals tab is now a year, month by month (year nav + 12-month strip that scrolls to the month), an Overdue drawer on top, and a «Renewed · income» pot at the bottom. «Renewed» writes `{at, value, due}` into `r.history`, sets `r.lastRenewed` and moves `r.date` one year past the covered date; Undo reverses it. Renewal income counts in the month it was PAID via `renewalsPaidForMonth()` — added to `collectedForMonth()` and `generatedForMonth()` (so the target and net profit include it). Year tab: «Money made» = new clients + monthly + renewals, and a «pots» row (Total collected / New clients / Renewals / Monthly subscriptions) above the expense cards; the PDF's Collected KPI includes renewals. Backup: `public/crm/index.backup-20260907-2.html`.
- **Public demo `public/crm-demo/index.html` (7 Sept):** rebuilt from the current CRM. It is the CRM file with the encrypted vault, login and sync stripped (`VAULT=null`, `#syncPill` hidden, no encrypted export / sync-setup menu items), a demo banner on top, the lock button turned into «Reset», and `demoSeed()` — a deterministic generator (mulberry32) that builds all sample data relative to today's date on every load (clients for the last 8 months + this + next, a year of call logs with stats counting from 1 Jan, renewals with history, subs, to-dos, ideas, callbacks, ASAP, targets). Nothing is persisted. Whenever the real CRM changes, regenerate the demo the same way rather than editing it by hand.
- **Callbacks 2.0 + Quick Log meeting chips (9 Sept, commit `e8310fa`):** Callbacks tab has a `#cbBanner` (`cbCounts()`: «Left to call now» = warm not done + scheduled overdue + scheduled due this month; warm / overdue / due this month / later / called-of-total), warm leads oldest-first with a waiting-days chip, scheduled grouped Overdue / this month / later with `s.phone`, `s.done`/`doneAt` and month chips; every row has one-tap outcomes — `cbOutcome()` writes a Quick Log entry for today (Booked also creates the Board client via `linkClientForLog`) and ticks the callback; Later/Now move between the two lists; called ones fold into a «Called» group (`state.meta.cbCalledOpen_*`). Home Attention has a 4th cell «Calls to make» (`cbNow`). Quick Log Booked card: `#qcDay` + `#qcTime` with `renderQcQuick()` chips (Today / Tomorrow / next 3 working days, `QC_TIMES`); `linkClientForLog(l,time,mday)` sets `c.day` to the picked day (else the logged day when only a time is given); log rows show a `.le-meet` chip from the linked client. Same edits applied to the demo + seed. Backups: `index.backup-20260909-2.html` in both folders.
- **Renewals: exact Papaki dates + «Lost · won't renew» (11 Sept 2026):** renewal dates now come from the client's Papaki domain — same day/month, year 2026 or 2027 (whichever is closer to the old estimate); `r.note` = «Papaki: <domain> · exact date». Domains live in two Papaki panels: controlpanel.papaki.com (Ημερομηνία λήξης tab, bought since May 2026) and www2.papaki.com/cp2 (older, `default.aspx?products=domain_names`, 111 domains, page 2 via postback). New `r.lost` / `r.lostAt`: a ⊘ button on each renewal row moves it to a collapsible «Lost · won't renew» section under the Renewed pot (Restore puts it back); `liveRenewals()` (= renewals without `lost`) feeds the banner, months, overdue drawer, Home overdue, Year `rTotal` and the year report — past `r.history` payments still count as income. Demo seed has two lost examples. Old dates kept in `state.meta.renewalDatesBackup_20260911` / `_20260911b`. Backups: `index.backup-20260911.html` in both folders.
- **Earned = new + renewals + monthly (13 Sept 2026):** `earnedForMonth(key)` → `{new, renew, subs, total}` (paid-stage clients + `r.history` payments + ticked subscription months) feeds the money strip, Home and Year; `generatedPartsForMonth/Year` gives the same split for Generated. The strip (`#strip`) now has New · paid (green) / Renewals (blue) / Monthly (violet, `#statSubsPill`) / Earned total (dark, three colour chips) / Expenses + net (`#statExpPill`) and is shown on Home, Calendar and To Do as well (Calendar hides the strip's month arrows via `body[data-view=calendar]` CSS). Home lost its Money hero group and the «High priority» card; «Booked meetings» (`.meetings-card`) lists every meeting from today on grouped by day. Year cards show «Earned» + New/Renewals/Monthly chips; banner Generated = `generatedForYear` with the split under it. Ticking a subscription month calls `renderAll()`. Expenses: `r.months[key]` overrides `r.amount` for one month (`recAmount(r,key)`, `recurringMonthly(key)`); Monthly tab click = this-month amount, ✎ = usual; one-time flag `state.expenses.claudeSept2026` sets Claude to €18 usual / €88 in 2026-09. New To Do tasks are created with `prio:'high'`. Backups: `index.backup-20260913.html` in both folders.
- **Online bookings → the Board (13 Sept 2026):** every meeting booked on the Zoho page or on Angelo's Google Calendar booking page is written into the Google Calendar «Advon Media» (advonmd@gmail.com). `app/api/bookings/route.js` reads that calendar through its private iCal address (Netlify env `GCAL_ICS_URL`, added 13 Sept), parses VEVENTs and returns clean rows; it authenticates with the CRM's own sync token (`x-crm-auth` vs `CRM_AUTH_HASH`, same as /api/crm) and answers 401 without it, so customer names/emails/phones are never public. Two booking shapes are understood: Zoho («Customer Info / Name : / Email : / Contact Number : / Booking ID :», summary «<service> with <name>») and the Google appointment schedule («<b>Κράτηση από</b>\n<name>\n<email>», summary «<schedule> (<name>)»); manual events and CANCELLED ones are ignored. In the CRM: `Bookings.pull()` on boot, on focus (2 min) and every 10 min; `state.meta.bk = {seen:{uid:'added'|'matched'|'skipped'}, auto:true}`; anything new dated today or later is put on the Board automatically as a Booked client with day, time, phone, email and `fromBooking:<uid>` (older ones wait for one tap), matching an existing client by email or name+day instead of duplicating; Home card `#bkCard` («Online bookings») shows what was added with Undo, the older ones with Add/Hide, the auto toggle and «Check now». The demo uses `window.DEMO_BOOKINGS`. CSP: `calendar.google.com` added to script/style/frame/connect-src in `next.config.ts` so a Google booking button or iframe can also be embedded on the site.
- **Committing to `main` from a Cowork session** (the Mac checkout is on `v2-draft`): stage the changed files into a temp dir, then `git read-tree main` → `git hash-object -w` + `git update-index --add --cacheinfo 100644,<blob>,<path>` per file → `git write-tree` → `git commit-tree <tree> -p main` → `git update-ref refs/heads/main <commit>` → `git read-tree HEAD` (restores the v2-draft index) → push `main:main` with the token. From macOS Claude Code simply `git switch main`.
- **Branch state since 6 Sept 2026:** `main` = the previous site + CRM 2.x (live at advonmedia.com; commit `2ef40f9` restored the pre-v2 tree). `v2-draft` = Design System v2 homepage, `/dorean-istoselida`, lead inbox and CRM 3.0.0, viewable at https://v2-draft--advonmediaa.netlify.app (Netlify branch deploy). The draft CRM/lead inbox use the data repo branch `draft` (Netlify env `CRM_GH_BRANCH` = `draft` for branch deploys, `main` for production), so playing with the draft never touches live data. To go live with v2: merge `v2-draft` into `main` (fast-forward is not possible — use `git merge v2-draft`), push, and set `CRM_GH_BRANCH` back to one value (`main`).
- Pushing: `~/Documents/ADVON-CLIENTS/.advon/push.sh ~/Desktop/advon-media` (reads the GitHub token from `ADVON-CLIENTS/.advon/secrets.env`; from a Cowork device shell the paths are `$HOME/mnt/ADVON-CLIENTS/.advon/push.sh $HOME/mnt/advon-media`). Never write the token into `.git/config`, a commit, or this file. Netlify API: `ADVON-CLIENTS/.advon/netlify.sh` (macOS only — the Cowork device shell cannot reach api.netlify.com; use Chrome on app.netlify.com there).
- After every push: check app.netlify.com → advonmedia.com shows «Published», then load the live page in Chrome, read console for `Refused|Content Security|rror`, check `/dorean-istoselida` + `/dorean-istoselida/efcharisto` (Zoho iframe must render) and run PageSpeed mobile.
- `lib/nav.js` holds the menu labels used by Header/Footer (keeps the 66KB catalogue out of the shared JS). Update it whenever `lib/services.js` nav labels change.
- Fonts are self-hosted via `next/font` (`lib/fonts.js`). A container without internet can build with `NEXT_FONT_GOOGLE_MOCKED_RESPONSES=<mock.js> npx next build --webpack` (Turbopack ignores the mock).
- Lead inbox: `/api/lead` reuses `CRM_GH_TOKEN` / `CRM_GH_REPO` (leads stored in `leads/inbox.json` of the data repo). Optional: `RESEND_API_KEY` + `LEAD_NOTIFY_TO`, `TELEGRAM_BOT_TOKEN` + `TELEGRAM_CHAT_ID`, `NEXT_PUBLIC_META_PIXEL_ID` — all set in Netlify → Environment variables.
- Device shell cannot delete files: before every git command `mv .git/*.lock _to_delete/`; replace files with `git show ref:path > path` (never `git checkout -- path`); move deletions into `_to_delete/` (gitignored).
- **13 Sept 2026 — CRM Leads tab, morning brief, intake, watchdog (commits `4cc4013`, `bc05f77`):** new tab «Leads» (`renderLeads`, `Leads.pull` ← `/api/pool` ← `leads/pool.json`), call mode with keys 1–4, No-answer parking, CSV import; Home «Cash forecast» (`cashForecast()`); `Brief.send()` after each sync → `POST /api/brief` → `brief/today.json` (plain); `c.stageAt` set on every stage change (board drop + editor). New routes: `/api/brief` (POST + GET, `?k=` text), `/api/pool` (GET), `/api/intake` (start / file / done, `?k=` list), `/api/watchdog` (`?k=` text); page `/ylika` (+ `components/intake/IntakeForm.jsx`); `netlify/functions/watchdog.mjs` scheduled daily. The read key lives in `ADVON-CLIENTS/.advon/secrets.env` (`BRIEF_READ_KEY`); its SHA-256 is hard-coded in the three routes. Demo CRM mirrors the tab with 48 seeded leads. Backups `index.backup-20260913-2.html`. See ADVON-CLIENTS/CLAUDE.md «Agents & automation».
- **14 Sept 2026 — sync 409 fixed at the root, bookings folded into Booked meetings, lead batches, /ylika v2, /dorean funnel:**
  - *Why the CRM showed «GitHub error 409 … is at X but expected Y»:* every save made three kinds of commit through the Contents API (data, snapshot, and dozens of background «prune snapshot» deletes), racing each other, the brief, uploads and the lead endpoint for the branch head. Fix: `lib/ghdata.js` — every server write is ONE commit built with the Git Data API (blobs → tree → commit → ref) and `commit()` retries the ref race up to 8× with jitter, rebuilding on the new head. `/api/crm` PUT = data + hourly snapshot (`snapshots/<YYYY-MM-DD>T<HH>.json`) + old-snapshot prunes in the same commit (≤120, at most every 6h). A real conflict (409 to the browser) now needs a NEWER copy from ANOTHER device (`baseUpdatedAt` + `deviceId`), a stale sha alone no longer blocks a save. `/api/brief`, `/api/pool` POST, `/api/lead` and `/api/intake` use the same helper. Browser side: `Sync.push` retries 3× quietly (1.5/3/6s, state «pending») before the red banner, and explains a 409/5xx in plain words.
  - *Bookings:* the Home «Online bookings» card is gone. Every calendar booking still to come goes straight on the Board (Booked meetings shows Zoho/Google tags via `c.bookingSrc`); older unmatched bookings are filed as `seen[uid]='past'` silently. Name matching is accent-insensitive and surname-subset (`bkSamePerson`: «Γουβιώτου» ⊂ «Σοφία Γουβιώτου»). `#bkRefresh` («Check calendar») and the status line live in the Booked meetings card; `#bkBody` shows only «Just in from the calendar» rows with Undo.
  - *Leads:* harvest leads carry `l.batch` (= `added` date); `ldListOf()` returns `new:<date>` so each harvest is its own category («Newly scraped · 21 Sept» = newest, highlighted; older = «Scraped 12 Sept»). Built-in `ads` category («From the ads», red 🔥) for form leads from the funnel — `/api/lead` writes them to `leads/inbox.json` AND `leads/pool.json` (`list:'ads'`, key `ad:<phone>`, `at`), the lead row shows «ads form · N min ago · CALL NOW».
  - */ylika:* three things only — last name, files, note (+ optional WeTransfer link). Upload flow: start (id only) → one loose blob per file (`POST git/blobs`, no commit) → done = ONE commit (`intake/<id>/meta.json`, `files/*`, `queue.json` with `note`, `link`, `list`). The CRM Home «Client material» card (`Intake`, `renderIntake`, `openIntake`) lists `queue.json` via `GET /api/intake?list=1` (x-crm-auth), shows thumbnails/downloads through `GET /api/intake?file=<path>`, and `POST /api/intake?status=1` marks Done. Agents' `?k=` text list unchanged (now includes the note).
  - */dorean* (`public/dorean/index.html`): the October ads funnel — one static file, no site chrome, Manrope via Google Fonts (CSP already allows), dark cinematic, one accent. Sections: hero + mockup stack, niche marquee, 3 steps, what's included, 9 sites from `/img/work`, price card (0€ build · 130€/yr or 15€/mo · after publishing 50€ per change request or 180€ admin panel), 12 verbatim Google reviews, FAQ, booking (Zoho embed in a modal, loads on click) + 3-field form → `/api/lead` (+ a best-effort Netlify Forms copy named `dorean-lead` for email alerts — enable Forms in Netlify), UTM capture, Meta Pixel via `META_PIXEL_ID` constant (empty = off). Offer wording rules: never «πληρώνετε μόνο αν σας αρέσει»; changes unlimited only until publishing. Backups: `index.backup-20260914.html` in both CRM folders.
- **14 Sept 2026 (later) — funnel v2: real portfolio, real reviews, Formspree, and the image bug:**
  - *Why images were missing on /dorean:* `public/img/work/` and `public/img/advon-logo*.webp` had never been committed (they came from the v2-draft work and sat untracked), so every mockup 404'd on the live site. Rule: assets referenced by anything on `main` must be `git add`ed — check `git ls-files public/img` after adding images.
  - Portfolio images are now generated from the real `lib/data.js` screenshots (raw.githubusercontent) with PIL: top-crop to 800:458, then webp q82 + avif q60 at 600/800/1400. 12 keys committed: allsmiles, plessa, fasoula, katranas, vasila, astra, tsonidou, finfinis, glynos, viw, bafakis, kontaxopoulou (~1.8MB total). Binaries reach the Mac as a base64 tar decoded with device_bash; the device shell cannot overwrite an existing file with `tar -x` (no unlink) — extract to `$HOME/tmpwork` and `cat new > existing` instead.
  - The funnel's hero stack and grid lead with **All Smiles (Άννα Στριγκόνη)** and **Μάρθα Πλέσσα**, as Angelo asked.
  - Reviews on /dorean are now the SAME Google cards as advonmedia.com: all 98 written reviews from `lib/reviews.js` verbatim, white cards, deterministic initial-avatar colours (same hash + palette as `ReviewsColumns.jsx`), gold stars, Google G, 220-char clamp with «Περισσότερα», 12 shown + «Δείτε κι άλλες» (+24).
  - Review count is **120+** everywhere now (site + funnel + `sats.jsx` counter + blog). Keep it in step with the live Google figure.
  - The interest form posts to **Formspree `https://formspree.io/f/xkopgoaj`** (the same endpoint as the site's contact form) AND to `/api/lead`; it asks for name, **email**, phone and specialty, and succeeds if either destination accepts. The Netlify Forms copy was dropped.
  - Every call-to-action says the 15-minute call is **δωρεάν** (header, hero, steps, price, modal header, FAQ entry).
- **25 Sept 2026 — client chat: the send button was broken on every phone since v6 (21 Sept); one-tap 🧪 Test chat in the CRM.** Backups `index.backup-20260925.html` in c, crm, crm-demo; `chat.backup-20260925.py`.
  - *The bug:* `sendBtn.addEventListener('click',send)` handed the click EVENT to `send(forced)` (added in v6 for the big buttons), so every tap on the arrow sent `{text:undefined, files:[]}` — the photo stayed in the tray, the server answered 400 «empty» and the client saw the red «Δεν στάλθηκε — πατήστε για επανάληψη» (retry hit the same wall). Enter on a desktop keyboard and the big stage buttons still worked, which is why it went unnoticed. Fix: the listener calls `send()` with no argument and `send()` ignores anything that is not `{text}`; `push()` refuses an empty message locally; server codes are translated to plain Greek (`ERR_EL`, network errors → «Χωρίς σύνδεση…»); the red label now shows «↻». Reproduced and verified with Playwright (`iPhone 13`, fake `/api/chat`): before = 400 + photo still in the tray, after = multipart upload then the message with the photo.
  - *Phone layout:* while a photo waits in the tray the chips and the ready card step aside (`.dock:has(.tray:not(:empty))`), the ready-card copy is two lines, and a ResizeObserver on the feed keeps the last message in view when the keyboard opens/closes or the bars fold (`atEnd`). Feed room at 390×667 in Υλικό: 206px → 311px.
  - *🧪 Test chat:* CRM → Chats → **«🧪 Test»** next to «New» (`chNewTest`) creates «Δοκιμή DD/MM HH:MM» with `demo:true` at Υλικό (welcome + Υλικό messages) and opens the link window («open this link on your phone and play the client»). The thread head keeps «↺ Restart at…». Same in crm-demo. `chat.py new "Name" --demo` does it from the shell. The «🧪 Test chat» checkbox in New/Edit still exists for turning any chat into a sandbox.
- **20 Sept 2026 (night) — Chats: priority + delete · clear messages to Angelo and to clients · Export = one full backup, one restore, one complete PDF.** Backups `index.backup-20260920-2.html` (crm, crm-demo), `chat.backup-20260920-2.py`.
  - *Chats list:* every row has a **⋯ menu** (also right-click): Make it a priority / Remove · Move up / Move down · Mark as unread · Edit details · Copy the client link · Archive · **Delete chat…**. Priority chats (`t.pin`, order `t.ord`, `t.pinAt`) sit in a «★ Priority» group on top and are **dragged to reorder** (`chOrder`, `chMovePin`, `Chats.pin/order/unread/remove`, `chRowMenu`, `#chMenu`); the thread head has a «☆ Priority» button, the Edit window a «Delete» button, Home «Client chats» lists priority first. API: `POST /api/chat?t=<slug>&meta=1 {pin, unread}`, `POST ?order=1 {slugs}`, `POST ?t=<slug>&remove=1` — one commit removes the card, `messages.json` and every file; a line stays in `chats/deleted.json` (`commitBefore` = the commit to bring it back from, the data repo keeps its history). The agent digest and `chat.py list` print «★ PRIORITY» first; `chat.py pin <slug> [--off]`.
  - *Deep link:* `advonmedia.com/crm/#chat=<slug>` opens that thread after sign-in (`Chats.wantOpen`) — every Telegram button and e-mail button to Angelo uses it.
  - *Messages (all built in `lib/chatcore.js`):* to **Angelo** — English like the CRM, the CLIENT'S NAME IN CAPITALS first (Greek capitals without accents, `upper()`), then the stage, then numbered blocks; Telegram is HTML (`sendTelegram(mc,text,{html,buttons})`, falls back to plain text if Telegram refuses the markup) with an «Open the chat» button; the e-mail is the same content in a designed card (`angeloMail`). Kinds: `clientWroteNote` (everything the client wrote since he was last told + files + «what happens next»), **`approvalNote`** (✋ APPROVAL NEEDED — 1 · what the client asked, 2 · what I did on the draft / what I intend to do, the draft link, 3 · the exact reply, what each button does), `quietNote`, `reminderSentNote`, `agentNote` (structured agent report: one block per client). To **clients** — one layout for every e-mail (`clientWrap`): dark header, title, a 4-step «ΠΟΥ ΒΡΙΣΚΕΤΑΙ Η ΙΣΤΟΣΕΛΙΔΑ ΣΑΣ» tracker, the message in a card (links clickable, `[label](#sheet)` → the chat page), one button, «Δείτε την ιστοσελίδα σας», a yellow «Επόμενο βήμα» line per stage, never a word about payment; `newMessageMail`, `reminderMail`, `plainClientMail` (outbox kind `client`).
  - *Approval flow:* the notifier has a step 5 — a card with `t.suggest` whose `at` is newer than `t.suggestNotifiedAt` gets ONE approval message (Telegram + e-mail); when the agent gave no `asked`, the client's last run of messages is quoted. `t.suggest` may now carry `asked`, `steps[]`, `draft`, `unclear`, `mode:'intend'`; the CRM's «Claude prepared this reply» box shows them too. `chat.py suggest … --asked "…" --step "…" --step "…" [--draft URL] [--intend] [--unclear "…"]`; `chat.py ping --title … --client "Name|slug" --line … ` builds a structured report. **Test for real:** `GET /api/chat?k=<CHAT_AGENT_KEY>&test=samples` sends one sample of every message (made-up client «Δοκιμή Πελάτης») to Angelo's Telegram and e-mail, including what a client receives.
  - *Export menu* = **Backup everything** · **Restore from a backup…** · **E-mail me a safety copy now** · **Complete report (PDF)** · «More» (Sync setup, Restart / Restore call statistics). The encrypted export/import, the plain JSON pair and the CSV button are gone from the menu (the restore still opens those old files). A red dot sits on Export when the last full backup is 14+ days old (`state.meta.lastFullBackup`, `renderBackupHint`).
  - *Backup everything* (`backupEverything`, `Zip` = a ~60-line zip writer/reader in the file, no library): one `ADVON-FULL-BACKUP-<date>_<time>.zip` with `READ ME FIRST - how to restore.txt`, `crm/state.json` (the whole state, plain), `crm/clients.csv`, `crm/renewals.csv`, `server/<path>` = EVERY file of the data repo except `snapshots/` (chats + attachments, leads, intake, brief, crm-data.json), `manifest.json` (counts, repo head, missing files). Server side: **`app/api/backup/route.js`** (x-crm-auth): `GET ?list=1` (one recursive tree call, `lib/backupcore.js listRepo`), `GET ?blob=<sha>`, `POST ?blob=1` (raw body → loose blob), `POST ?restore=1 {files:[{path,sha}]}` (ONE commit; only `chats/ leads/ intake/ brief/`; never crm-data.json), `POST ?mail=1`. Measured 20 Sept: 42 files, 4.3 MB, nothing over the 5.5 MB single-file limit.
  - *Restore* (`#restoreAnyFile` → `restoreFromZip` / `restoreConfirm` / `runRestore`): shows backup-vs-now counts, two ticks — «Put back the CRM» and «Also put back the chats, leads and client files on the server» (off by default) — downloads `advon-crm-BEFORE-restore-<date>.json` first, then `state=…; normalizeState(); saveNow()`, then uploads blobs and commits. **To restore for Angelo from a backup zip without the CRM:** unzip, push everything under `server/` to the same paths of the data repo (`.advon/data.py put`), and have him restore `crm/state.json` through the menu (the CRM re-encrypts it with his passphrase; `crm-data.json` inside the zip is the locked copy and needs that passphrase).
  - *Extra net:* `netlify/functions/weekly-backup.mjs` (Mondays 05:30 UTC) → `sendSafetyCopy` e-mails `ADVON-SAFETY-COPY-<date>.zip` (~300 KB: crm-data.json locked, every chat's messages, leads, brief, intake queue; no attachments) to `LEAD_NOTIFY_TO` through Resend. Same thing on demand from the menu or `POST /api/backup?k=<CHAT_AGENT_KEY>&mail=1`.
  - *Complete report* (`exportYearReport`, same print-to-PDF engine): cover with Earned / New / Renewals / Monthly / Still to collect / Net + contents; 1 · year in numbers (stacked chart, month table with New paid · Renewals · Monthly · Earned · Still owed · Expenses · Net, all-years table); 2 · pipeline; 3 · **month by month** (money line, every client with value / paid / owed / deposits / extras / flag / meeting / contact / notes, the month's renewals with exact date + status + Papaki note, subscriptions collected, trash, didn't convert); 4 · money still to collect + Trash; 5 · renewals by exact date grouped by month, renewed income, lost; 6 · subscriptions 12-month ✓ grid; 7 · expenses per month; 8 · calls & meetings; 9 · callbacks (Call first / warm / scheduled); 10 · client chats; 11 · leads; 12 · to do & ideas. Report for another year = pick the year on the Year tab first.
  - Tests used: Playwright against `public/crm-demo` with `/api/backup` mocked by `page.route` (backup → zip checked with Python `zipfile` → restore → bytes identical), and `lib/backupcore.js` run from the Mac shell against the real data repo (read only: `NODE_USE_ENV_PROXY=1 node $HOME/work/bk/t.mjs`).
- **20 Sept 2026 (evening) — one click = half, the chat stops asking for e-mails, four client chips.**
  - *Part payments are now ONE click.* Half up front is what nearly every client does, so: Remaining row → «½ Half now» writes half straight away (note «προκαταβολή»), and the client card leads with a green «½ Half up front · €X» button plus «Paid in full · €Y»; the amount/date/note row hides behind «+ another amount». The pay-pop-over is gone. Every money write now lands in a toast with **Undo** (`toastUndo`, `undoPayment` restores the previous stage too); a background toast (`toast(msg, true)` — board sync, new chat message, e-mail fill) never steals an Undo that is still on screen.
  - *No e-mail question on the client page.* Angelo has every client's e-mail from the meeting and fills it in himself, so the «Να σας ειδοποιούμε;» bar, its CSS/JS and the `?c=…&contact=1` endpoint are all removed (nobody with a link can set an address on a card any more). The CRM keeps the «⚠ no e-mail · no alerts» warning and the Telegram quiet-ping, and now **fills the chat's e-mail from its linked Board card by itself** (`chSyncBoard` → `Chats.meta`, once per thread per session, only when the chat has none).
  - *Client chips — the same four everywhere* so the client learns one row: 📷 Στέλνω υλικό · ✏️ Θέλω αλλαγές (numbered 1./2./3. list) · ✅ Πάμε live — έτοιμο (says they approve AND asks for 2–3 domain names, π.χ. epitheto-epaggelma.gr) · ❓ Έχω ερώτηση. On `live`: Θέλω μια αλλαγή · Στέλνω υλικό · Έχω ερώτηση · Κριτική στο Google. The caret lands on the first «1. » so they just start typing.
- **20 Sept 2026 — part payments, Chats v5 (approve with one tap, client e-mail, Board follows the chat), layout polish.** Backups `index.backup-20260920.html` in crm, crm-demo and c.
  - *Part payments:* `c.pays=[{id,amount,date,note}]`; `cpaid(c)` (whole value once the stage is Paid, else the deposits, capped at the value) and `cowed(c)` feed EVERY money figure — `newPaidForMonth` = Σ cpaid over all the month's clients (a deposit from a client later lost/trashed stays counted: `lostPaidForMonth`), `monthMoney` collect / potential / risk = Σ cowed, Remaining, Home «People to contact», cash forecast, the brief (`value` = owed, plus `total`, `paidSoFar`), Year grid, year report and the clients CSV (Paid so far / Still owed). UI: «Money received so far» block in the client modal (Half up front / The rest chips, progress bar; a typed-but-not-added amount is still saved; everything received → the card moves to Paid by itself), «½ Part» button on every Remaining row (`openPayPop`), Board chip «€65 / €130» with a green fill, `addPayment(id,amount,date,note)`.
  - *Bug fixed — pop-ups taller than the screen:* `.modal` had no max-height, so on a laptop the client card's Save row sat below the screen. Now `.modal` is a flex column capped at `100dvh - 40px`, head and foot fixed, `.modal-body` scrolls. Applies to every overlay.
  - *Bug fixed — UTC month:* `curMonth` and the «current month» highlights used `toISOString()` (UTC), so between 00:00 and 03:00 Athens time on the 1st the CRM still showed last month. All six places now use `todayIso().slice(0,7)`.
  - *Chats → approve with one tap:* the agent stores `t.suggest={text,plan,stage,at,forMsgAt}` on the card (`chat.py suggest`); the CRM thread shows «Claude prepared this reply» above the box — **Send it** (sends as Angelo, then applies `stage`), **Edit first**, **Dismiss** (`POST ?meta=1 {suggest:null}`); any outgoing message deletes the suggestion server-side. List chip «reply ready», header hint «N ready to send».
  - *Client e-mail:* none of the first real chats had an e-mail, so no client was ever told about an answer and no reminder could go out. The client page now asks for it once («🔔 Να σας ειδοποιούμε όταν απαντάμε;» → `POST ?c=<code>&contact=1 {email}`, never overwrites an address Angelo typed; `publicThread.hasEmail`), the CRM shows «⚠ no e-mail · no alerts» in the thread head and «link not opened» when `clientSeenAt` is empty after 20 h, and the notifier pings Angelo on Telegram once when a thread without e-mail has been quiet 3+ days (`quietPingAt`).
  - *Notifier manners:* greeting follows the Athens clock (`greeting()`: Καλημέρα before 13:00), reminders only 09:00–20:00 Athens and never on Sunday (`politeHour()`), the outbox write merges by id inside the commit so a ping queued meanwhile is not lost.
  - *No double messages:* both pages send `cid` (the pending id); `appendMessage` returns the stored twin instead of writing again (`duplicate:true`).
  - *GitHub rate limit:* the head memo in `lib/chatcore.js` is 4 s (was 1.5 s) and the client page polls 5 s / 15 s / 45 s — a dozen clients online together no longer approach GitHub's 5,000 calls/hour.
  - *Client page:* a half-written message survives a reload (`localStorage advon_c_<code>_draft`, try/catch), `enterkeyhint=enter` (on phones Enter is a new line). CRM composer: Enter sends only with a real keyboard.
  - *Board follows the chat:* `chSyncBoard()` after every list change — a chat is tied to its Board card when exactly ONE live client matches the name, Greek or Greeklish (`chSameName`: «Σφυρή Αλεξάνδρα» = «SFYRI», «Θωμά» = «THWMA»), remembered in `c.chat`; the card then moves FORWARD only (Υλικό → Need info, Πρώτο draft → Draft · Review, Αλλαγές → Making changes), never into or out of Waiting payment / Paid / Thinking / Lost. Board cards carry a chat chip (red «waiting» when the client wrote last) that opens the thread. `chThreadFor` is a hoisted function with try/catch because the Board renders before the Chats block of the file has run.
  - *Layout:* between 901 and 1780 px the tabs take their own full-width row (they used to push the search onto a lonely second line); the money strip is one aligned grid on desktop and a compact 2-column grid on phones (the minis scroll sideways), which brings the Home greeting back onto the first phone screen.
  - *Agent side (ADVON-CLIENTS):* `chat.py suggest|unsuggest`, `list` shows READY threads; `.advon/draft.sh save|status|publish` = the Netlify credit rule ([skip netlify] commits, publish once); mode file now `review`; ONE scheduled task «Advon — Chat agent (09:05 & 17:05, needs the Mac)» — the old 3×/day one is switched off. **Every Mac task created from a cloud session comes out «cloud only» until Angelo turns on «Require this computer» for it in the desktop app — that is why the runs since 18 Sept finished in ~20 s having done nothing.**
- **18 Sept 2026 (later) — money boxes, people to contact, call-first, draft templates.**
  - *Strip:* `monthMoney()` feeds a green **New money this month** box (`#statNewPill`: the sum on top, then collected green + still owed red) in place of the old to-collect pill; **In the bank** is the last box in the row and its figure is green. `SITE_MIN = 120` — anything cheaper is another service, so it is out of the client count and the conversion stats.
  - *Home:* one **People to contact** card (`#homeCollect`, `.chase-*`): about to pay (green flags), most likely lost (orange/red), overdue renewals, each row with a tap-to-call number and a click through to the client; the separate «Overdue renewals» card is gone (`#homeRenew` is guarded). Renewals due within 7 days render themselves at the top of **Do ASAP** (`.asap-row.auto`).
  - *Callbacks:* `state.prio` + `#cbPrioPanel` — a **Call first** list; rows in every list are `draggable` and `cbDrag` carries {list,id} into the panel's drop handler. It leads `renderCbBanner`.
  - *Elsewhere:* the calendar opens on the month (`calMode='month'`), the Year banner counts clients paying **now** (live renewals + monthly subs), and the Renewals «Lost · won't renew» box is always on screen.
  - *Chats:* three **First draft** quick replies carry the real e-mail wording and the three Stripe links (plain · with contact form · with articles). `chFill()` fills `{site}` and `{name}`. That message is the only place money is mentioned — CLAUDE.md «v4» in ADVON-CLIENTS and the chat-agent task prompt say the same. The client page has a permanent «Υλικό» button beside «Οδηγός αλλαγών».
- **18 Sept 2026 — Chats v3 + the money strip.**
  - *Client page:* a client can delete their own message (tap their bubble → bin) — `POST /api/chat?c=<code>&del=<id>` tombstones it (`deleted:true`, text and files cleared, counters recomputed); the CRM can delete any message with `?t=<slug>&del=<id>`. The thread now renders keyed (a poll patches only what changed instead of rebuilding `innerHTML`), uploads run three at a time, and the first paint stays pinned to the bottom until the web font has settled (`stickBottom`).
  - *Sheets:* «Οδηγός» → «Οδηγός αλλαγών», plus a new «Τι χρειαζόμαστε στο «Υλικό»» sheet (the four things we need, the doctoranytime shortcut, WeTransfer over 4 MB, how to describe a website they like, the timeline). A message can link to one with `[text](#odigos|#extras|#yliko)` — `linkify` turns that into a chip; the CRM shows the label.
  - *Timings everywhere:* first draft **5–14 days**, a clear round of changes **within 24 h**, live **1–2 days** after approval.
  - *New chats send two messages* — welcome + what we need at this stage (`CH_WELCOME` + `CH_YLIKO` in the CRM, `WELCOME` + `YLIKO_MSG` in `.advon/chat.py`); `POST ?new=1` accepts `welcome` as a string **or an array**.
  - *CRM money strip:* `monthMoney(key)` → **In the bank** (received) · **Still to collect** (agreed, under way, unpaid) · **Generated** (the two added) with **potential** (booked + thinking + trash) faded as `+€` and a ghost segment on the target bar. Renewals / Monthly / Costs / At risk are small boxes (`.mini`). `statPaid` and `statTotal` are gone.
  - *Home:* the «Client material» card is removed (`Intake` code stays, guarded); «Subscriptions to collect» lists every monthly client unpaid **for the current month**, overdue first. A minute timer follows the real date past midnight so Quick Log and call mode always log on today.
- **16 Sept 2026 — Chats (commit `932a945`):** client conversations inside the CRM.
  - *Where:* `app/api/chat/route.js` (all reads/writes), `public/c/index.html` (the client page, one static file — `/c/<code>` is rewritten to it by netlify.toml, with a Next `rewrites()` fallback), the CRM «Chats» tab + Home «Client chats» card (`Chats`, `renderChats*`, `chOpenModal`, `chShowLink` in `public/crm/index.html`), and the demo mirror seeded from `window.DEMO_CHATS`. Backups `index.backup-20260916.html` in both folders.
  - *Data:* private data repo `chats/index.json` (one card per thread: slug, `code` = the secret in the client link, name, phone, email, site, stage `yliko|draft|changes|payment|live`, lastAt/lastFrom/lastText, `unreadAdmin`/`unreadClient`, seen times, archived) + `chats/<slug>/messages.json` + `chats/<slug>/files/`. One commit per write via `lib/ghdata.js`; reads are cached per function instance keyed by the branch head, so a poll that finds nothing new is one GitHub call (`?h=<head>` → `{same:true}`).
  - *Auth:* client page = the code in the link only (rate-limited; unknown/archived code → 404); CRM = `x-crm-auth`; agents = `?k=` — the BRIEF_READ_KEY reads (`GET ?k=…` plain-text digest of threads waiting for an answer, `&t=<slug>` one thread, `&format=json`), the **CHAT_AGENT_KEY** (secrets.env, SHA-256 hard-coded in the route) may also `POST ?k=…&t=<slug>` `{text}` as `from:'claude'` — the client sees it as Advon Media, the CRM tags it «Claude».
  - *Alerts:* client message → Telegram (`TELEGRAM_BOT_TOKEN`/`TELEGRAM_CHAT_ID`) and/or e-mail to `LEAD_NOTIFY_TO` via Resend; Angelo's reply → e-mail to the client's address (skipped when the client polled the page in the last 3 min). **None of these env vars is set in Netlify yet** (only the CRM_* ones and GCAL_ICS_URL) — until `RESEND_API_KEY` (and optionally the Telegram pair) is added, the only signal is the badge on the Chats tab / the Home card.
  - *Polling:* client page 4 s active / 12 s idle / 45 s hidden; CRM list 20 s on the tab (90 s elsewhere), open thread 5 s. Netlify credits (Pro, 3,000/month): web requests cost 2 credits per 10k, compute is negligible; **a production deploy costs 15 credits** — that is the real spend (27 deploys = 405 credits in the first 3 days of the Sept 13 period), so batch pushes; chat traffic never triggers a deploy.
  - *Threads created 16 Sept:* sfyri (Σφυρή Αλεξάνδρα), ntonika, thwma, plus `dokimi` (test). New ones come from the CRM «New» button (creates the code and a welcome message, shows the link + a ready Viber text).
  - **v2 (commit `707e6ff`, same day):** stages are `yliko → draft → changes → live` (payment is due at the first draft, not a stage; old `payment` reads as `changes`). `lib/chatcore.js` holds the shared pieces: fast reads (one `git/trees/<chats>?recursive=1` per branch head + blobs cached by sha) and the Resend/Telegram senders + Greek templates. Notifications moved out of the route into `netlify/functions/chat-notify.mjs` (every 5 min): client message → Telegram + e-mail to Angelo (`angeloNotifiedAt`), Angelo/Claude message → e-mail to the client with the text (`clientNotifiedAt`, skipped if they already saw it), silence 3+ days after we wrote → stage-aware reminder (`remindersSent` ≤ 2, `lastReminderAt`), plus `chats/outbox.json` (pings queued by the agent, which can only reach GitHub). From address: `CHAT_FROM` (default «Άγγελος · Advon Media <angelos@advonmedia.com>» — needs the domain verified in Resend), reply-to `CHAT_REPLY_TO` (default advonmd@gmail.com). Client page: E-mail (advonmd@gmail.com) and «Οδηγός» buttons (examples of what to ask and how), stage hint line, quick-reply chips per stage. CRM: threads are prefetched so opening is instant, changing the stage pre-fills the matching message, a log strip shows agent/system entries (`t.log`). Agent side: `ADVON-CLIENTS/.advon/chat.py` + the «Chat agent» playbook in ADVON-CLIENTS/CLAUDE.md; scheduled task «Advon — Chat agent» 09:00/13:00/18:00 Athens, mode in `.advon/chat-mode.txt` (`confirm` now).
  - **Notifications live (16 Sept 2026, evening):** Resend domain advonmedia.com verified via 3 records at **Squarespace** (DNS host — not Hostinger/Papaki): TXT `resend._domainkey`, MX `send` → feedback-smtp.us-east-1.amazonses.com (10), TXT `send` → `v=spf1 include:amazonses.com ~all`; Resend's inbound MX on `@` was deliberately NOT added (Hostinger keeps the mailbox). Squarespace asks the owner (agelmet7@gmail.com Google login) to re-verify before DNS edits. Netlify env: `RESEND_API_KEY` (key «advonmedia-chat», sending only), `TELEGRAM_BOT_TOKEN` (bot **@AdvonMedia_bot**), `TELEGRAM_CHAT_ID` 6829584100. Tested end to end: client email and Angelo email both delivered. Test switch: `GET /api/chat?k=<CHAT_AGENT_KEY>&test=telegram` (diagnosis: bot name + which chats have written to the bot; if exactly one chat wrote and the env id is refused, that chat is used) and `&test=email[&to=…]`. Scheduled functions cannot be opened by URL in production — use these.
- **14 Sept 2026 (evening) — funnel polish:** `--max` is 1340px and the hero is `1fr 1.26fr`, so the mockup is ~670px wide and the Google proof line now sits UNDER it in a `.hero-side` column (both columns carry weight instead of the image floating in empty space); the second hero CTA is a quiet `.cta-alt` link, not a button, so everything stays above the fold at 1728×996. Effects layer added, all transform/opacity, all off under `prefers-reduced-motion`: reading-progress hairline, a hero starfield canvas (sprite-based, 30fps, paused off-screen — matches the brand's main site), a pointer spotlight, the stack tilting on `--rx/--ry` with a scroll parallax on `--py`, a shimmering headline gradient, a floating 0€ badge, a pulsing ring on the main CTA, a count-up stat band under the marquee (200+ / 120+ / 5.0 / 0€ counting down from 1.200€), 3D tilt + glare on the portfolio cards, magnetic buttons, staggered reveals and review cards. Links: portfolio → `advonmedia.com/kataskevi-istoselidas#portfolio` (that anchor already exists in `KataskeviClient.jsx`), nav label «Portfolio», and every «read the reviews» link — on the funnel AND on the site (`lib/reviews-meta.js` + `lib/reviews.js` `REVIEWS_URL`) — now points at Angelo's Google reviews search URL instead of the short share.google link.
- **21 Sept 2026 — Chats v6: one button per stage, the client tells us when to start; doctoranytime clients send only photos; «Domain connected» sends the review ask; first draft 5–10 days everywhere.** Backups `index.backup-20260921.html` in crm, crm-demo and c; `chat.backup-20260921.py` in ADVON-CLIENTS.
  - *Why:* the process must feel frictionless — the client should not research sites or write texts for weeks; we build first, they check. So the page now says exactly one thing per stage and has one big button that tells us «go».
  - *Client page (`public/c/index.html`):* a `#ready` bar in the dock — Υλικό: «✅ Ξεκινήστε την ιστοσελίδα μου» (confirm sheet → message `kind:'ready:build'`); Πρώτο draft / Αλλαγές: «✏️ Έστειλα όλες τις αλλαγές» (confirm → `ready:changes`) and «🚀 Όλα καλά — πάμε live» (a sheet asking 2–3 domain wishes → `ready:live`, text lists them 1./2./3.); after a press the bar turns into a green «το λάβαμε» card until the stage moves on or we clear the flag; Live: no bar. A half-written message is sent first (`pressReady`). Button messages are green bubbles (`.bub.kind`). Stage hints are per stage (`stageHint`), and `t.dat` (doctoranytime) switches the Υλικό hint/bar to «only photos, here or advonmd@gmail.com» and drops the «Στέλνω στοιχεία» chip. Sheets: nothing compulsory, temporary photos OK, 5–10 days, the buttons named in the timeline. `publicThread` now carries `dat` and `ready`.
  - *API (`route.js`):* client POST `{kind:'build'|'changes'|'live'}` → `msg.kind='ready:<k>'` and `t.ready={kind,at,id}`; a stage change (meta) or `meta {ready:null}` or an advon/claude message with `{clearReady:true}` removes it; `meta {dat:true|false}` and `new {dat}`. The `?k=` digest prints ✅ READY TO BUILD / READY FOR CHANGES / WANTS TO GO LIVE + [doctoranytime], plus a STILL FLAGGED list; `thread` lines show `[BUTTON ready:…]`. `clientWroteNote` (Telegram/e-mail to Angelo) leads with the button pressed and says what happens next (green badge).
  - *CRM:* thread head has «doctoranytime?» / «✓ doctoranytime» toggle (`t.dat`), a green READY badge with «done ✓» (`meta {ready:null}`), and **«🌐 Domain connected»** (`chDomainConnected`: prompt pre-filled with the domain from the client's «πάμε live» message via `chDomainAsked`, confirm → `meta {site, stage:'live', ready:null}` + sends `CH_DOMAIN_LIVE` = Angelo's exact review-ask text with the domain and https://g.co/kgs/wXBAZWP). List/Home chips show «Ready to build / Ready for changes / Wants to go live». New-chat modal: «Has a doctoranytime profile» tick swaps the second welcome (`CH_YLIKO_DAT` vs `CH_YLIKO_PLAIN`). Templates rewritten: `CH_WELCOME` (short, names the buttons), the three «First draft» replies (unlimited changes if they want any + the two buttons, no domain question — it comes with «πάμε live»), «Changes done · check + two buttons», «Domain connected · review ask», two nudges («press Ξεκινήστε» / «press Έστειλα όλες τις αλλαγές»), «Need photos only»; `CH_STAGE_MSG={draft:1,changes:4,live:5}`; `chFill` also fills `{domain}`. «Send it» on a suggestion with `suggest.done` clears the flag. Demo seed: nikolaou `dat:true`, lampridi pressed «πάμε live».
  - *Agent (`ADVON-CLIENTS/.advon/chat.py` + CLAUDE.md «v6» + the scheduled-task prompt):* `list` prints the flags; `new --dat`; `meta dat=1`; `reply --done` / `suggest --done` / `done <slug>` clear the flag; `stage` clears it. Rule: build only with READY TO BUILD, change only with READY FOR CHANGES, otherwise send the one-line nudge once; WANTS TO GO LIVE → check the domain and tell Angelo, he presses «Domain connected».
  - *5–10 days:* `lib/services.js`, `lib/home.js`, `app/faq/page.jsx`, `public/dorean/index.html` (was «2–3 εργάσιμες» on the funnel — five places), chat sheets/FAQ/templates, `lib/chatcore.js` NEXT_STEP + reminders.
- **21 Sept 2026 — Growth package + the clear «Τι παίρνετε» row (live; Angelo approved the preview the same day).** Growth has the same Ετήσια/Μηνιαία toggle as hosting (ONE shared `billingAnnual` state, so both cards always show the same cycle): **39.58€/μήνα = 475€/χρόνο prepaid** («Εξοικονομείτε 113€ τον χρόνο») or **49€/μήνα** (588€/χρόνο). `components/service/KataskeviPricing.jsx`: a five-tile checklist above the prices (`GET_LIST`: design from scratch · mobile first · ready for Google · EL/EN · draft in 5–10 days), then two equal cards — Φιλοξενία (the annual/monthly toggle now lives inside the card, same prices and wording, plus `HOSTING_LIST`) and **Growth 49€/μήνα +ΦΠΑ** (`GROWTH_LIST`: everything in hosting + 1 SEO article a month, articles section included, Google Analytics setup, traffic monitoring, phone-tap + form tracking, monthly report with suggestions, priority on changes; badge «ΝΕΟ», «Και για ιστοσελίδες που έχουμε ήδη φτιάξει»). Wording rule: the idea came from agenths.gr's Growth plan but the list is ours — no «Server Updates», «Security Monitoring» or «call tracking» (we count taps on the phone button). CSS: `.get-tile`, `.growth-card` in `app/globals.css`. Open points for Angelo: minimum commitment for Growth, whether the 70€ articles section is really free inside it, and the monthly article/report must be produced by a scheduled task per Growth client (not built yet). Local preview without Google Fonts: copy the repo to `$HOME/work/site`, `npm ci`, build with `NEXT_FONT_GOOGLE_MOCKED_RESPONSES=$HOME/work/fontmock.js npx next build --webpack` (a Proxy module that answers every URL with one @font-face), then serve `.next/server/app/<page>.html` + `.next/static` as `/_next/static`.
- **21 Sept 2026 (later) — Growth v2 + the «Γιατί όλα αυτά;» pop-up (Angelo picked the features himself).** `GROWTH_LIST` now promises: **3 articles a month** (was 1), the articles section, **1 free change a month (worth 50€)**, **Google Search Console (which searches find them + position)**, Analytics + traffic (merged into one line), phone-tap/form tracking, monthly report, priority. He declined (for now) ready-made Google review replies and social post texts. `components/service/GrowthWhy.jsx` = the dashed «Γιατί όλα αυτά;» button under the Growth list + the window it opens (portal to `document.body` because the card/ScrollReveal are transformed; Esc, backdrop click, focus trap, focus returns to the button, page scroll locked; bottom sheet on phones). Content: 4 reasons, a «Πόσο κοστίζουν αυτά αλλού» comparison with growing bars (agency SEO 150–900€/month, 3 copywriter articles ≈100€/month, 1 change 50€ vs Growth at the price of the selected billing cycle), «Πώς γίνεται σε αυτή την τιμή;», and an honest line that we never promise first place on Google. Market figures checked 21 Sept 2026 (cosmolink.gr, imagenwebpro.com, seo-keimena.gr 0.07€/word) — no company is named on the site; re-check them if the pop-up is still live in 2027. CSS: the `.why-*` block at the end of `app/globals.css` (transform/opacity only, off under reduced motion). **Delivery rules for Growth clients:** the 3 articles are produced once a month in one batch (one push per client site), medical/legal articles go to the client for an OK in their chat before publishing, Search Console must be verified for the client's domain at onboarding. The monthly scheduled task that does this per Growth client is STILL not built — build it before the first Growth client pays. Device-shell note: background processes die when a device_bash call ends, so `npm ci` and `next build` must each finish inside one call (both do, ~35 s and ~60 s); to screenshot, tar `.next/server/app/<page>.html` + `.next/static` into `_to_delete/`, stage it and run Playwright in the cloud container.
- **21 Sept 2026 (evening) — Growth wording fixes from Angelo:** (1) never show a date on the price comparison (no «Σεπτέμβριος 2026») — just «Ενδεικτικές τιμές της ελληνικής αγοράς»; (2) the free monthly change is ONE CHANGE REQUEST = one complete set of instructions for everything they want changed, not one single change — card line «1 αίτημα αλλαγών κάθε μήνα, χωρίς χρέωση (αξίας 50€): όλες οι αλλαγές που θέλετε, σε ένα μήνυμα»; (3) never tell clients our work is automated — «Πώς γίνεται σε αυτή την τιμή;» now says we know everything has become more expensive and we want to help small businesses and professionals, so we keep the price low.
- **21 Sept 2026 (night) — chat page: contact-form upsell + phone layout (commit `79adfc1`, not pushed yet — waiting for «put it live»).** The 50€ Φόρμα Επικοινωνίας is offered quietly on `public/c/index.html`: a small card under the conversation from the draft stage on («Τη θέλω» → confirm sheet → client message `FORM_TXT`; «Όχι, ευχαριστώ» hides it on that device, `advon_c_<code>_formNo`), an unticked add-on line in the «πάμε live» sheet, and first place in Extras with a «Η πιο συχνή επιλογή» badge. Never shown when the card has `t.form` (CRM thread head «contact form?» toggle → `meta {form}`; `publicThread` carries `form`; digest shows `[has contact form]`) or once the client asked. Phones (≤560px): smaller type, 34px icon header, compact steps, stage hint clamped to 2 lines (tap opens), ready buttons side by side, no footer hint, and `body.typing` hides steps/hint/ready while the keyboard is up. Wording: «Έστειλα ό,τι ήθελα», received card «3–10 ημέρες». Fixed: `.ready[hidden]` (an empty bar showed on Live). Backups `index.backup-20260921-2.html` in c, crm, crm-demo.
- **23 Sept 2026 — Client reminders · Board → chat by itself · the demo chat is a sandbox · Today pop-up · Pay now ⇄ Remaining · compact cards everywhere (branch `chats-v7`, LIVE 23 Sept 2026 — Angelo said «push live»).** Backups `index.backup-20260923.html` in crm, crm-demo and c.
  - *Client reminders* («remind KOURAKOS on Friday for the material»): the store is Angelo's Google Calendar «Advon Media» — an event titled **`⏰ CLIENT — what we need`** (09:00–09:15, description may carry a paragraph starting `Viber:` = the ready text for the client; Claude writes it at capture time). `lib/reminders.js` parses the ICS (same `GCAL_ICS_URL` as bookings; `/api/bookings` ignores these events, so they never reach the Board), `app/api/reminders/route.js` (x-crm-auth `GET` → items with `when` overdue/today/upcoming, `POST ?done=1 {uid[,undo]}`; `?k=<BRIEF_READ_KEY>` → plain text) and `netlify/functions/reminders.mjs` (05:30 UTC daily: every open reminder due today or earlier → ONE Telegram + e-mail to Angelo with the Viber text per client, repeated each morning until ticked, never twice a day). Bookkeeping `reminders/state.json` {done, sent} in the data repo (the calendar is read-only for us). CRM Home card «Client reminders» (`Reminders`, `renderHomeReminders`): Overdue / Today / Next 14 days / Done this week, «Copy Viber text», «✓ Done» (Undo toast), «+ New» opens Google Calendar pre-filled (`remNewUrl`), «Check calendar». Demo: `window.DEMO_REMINDERS`.
  - *Board → chat:* `chAutoCreate()` (run at the top of `chSyncBoard`, which the drop handler and `reviveClient` now always call): a card in Need info / Building / Draft·Review / Making changes with no chat and `stageAt ≥ state.meta.chatAutoSince` (set once by `migrate20260922`) gets a chat with the card's name/phone/e-mail (`CH_AUTO` = needinfo→yliko, building→yliko, review→draft, changes→changes); a same-name chat is linked instead. `POST ?new=1` now accepts `building:true` (→ `ready={kind:'build', manual:true}`: the client page shows «το λάβαμε, έρχεται το draft», the agent digest / `chat.py list` print «🔧 … set from the Board … do NOT build»), `log`, `demo`. No e-mail → the thread shows an orange **«I need X's e-mail»** bar (`.ch-askmail`, saves to the chat AND the Board card). A chat born at First draft / Changes carries only the welcome; opening it puts the matching quick reply in the box (`chPrefillNew`).
  - *Demo sandbox:* card flag `demo:true` (Edit modal tick «🧪 Test chat»; `paradeigma-deigma` flagged in the data repo). Thread head: «↺ Restart at…» (Υλικό / Υλικό·doctoranytime / Πρώτο draft / Αλλαγές / Live) → `POST ?t=<slug>&reset=1 {stage, dat}` wipes messages + files and seeds the stage's intro (`lib/chatdemo.js demoSeed`). A client message on a demo thread gets an instant scripted answer (`demoReply`, appended as Angelo ~1 s later): «Ξεκινήστε» → the First-draft message (site = allsmiles.gr as a stand-in) + stage draft; «Έστειλα όλες τις αλλαγές» → «Changes done» with the client's numbered lines ticked + stage changes; «πάμε live» → «domain free» then the domain-connected/review message + stage live; plain text → the stage nudge. Client page shows a yellow «🧪 Δοκιμαστική συνομιλία» band (`publicThread.demo`). Demo threads: skipped by chat-notify.mjs, by the agent digest (`?k=`), by `chat.py list`, and by `chWaiting`/`chUnread` in the CRM.
  - *Do ASAP from anywhere:* a calendar event titled **`📌 <task>`** (🔥 anywhere in the title = super-high) is added to «Do ASAP» once its day has come (`parseAsap` in lib/reminders.js → `asap` in `GET /api/reminders` → `asapImport()` in the CRM, top of the list, `from:'calendar'`, remembered in `state.meta.asapIn` so it never comes twice). Demo: `window.DEMO_ASAP`.
  - *«Today» pop-up:* the first time the CRM opens (or comes back to the front) on a new day — any tab — one window (`#dayOverlay`, `openDay`/`maybeDay`/`closeDay`): ⏰ reminders due today or still open (Copy Viber text / ✓ Done), today's meetings from the Board (Call / Open), then every open «Do ASAP» item, 🔥 first (✓ Done with Undo). «Got it», ✕, Esc or the backdrop close it and set `state.meta.daySeen=today` (synced → once a day across devices). No pop-up on a day with nothing in it. Reopen: Home → Client reminders → «Today». Runs after `Reminders.pull` (so the calendar is read first) and never on top of another open window.
  - *To Do «Pay now» ⇄ Remaining:* `TODO_HOSTS` gained a host with a `pick` (`paynow`: unpaid + green flag + owes money, `money:true`) hosted by the «Pay now» lane (`state.todoHosts.paynow`); its rows show «€X to collect» + stage and a permanent «✓ Paid» (`markPaidFromRemaining`); the lane head shows the SUM owed (`.tl-sum`). They leave the lane by themselves when paid / trashed / the flag changes. Dropping a client on that lane does nothing (no stage behind it).
  - *Remaining + Renewals as small cards:* `.rem-month-body` / `.rn-body` / `.rn-pgroup .rows` / `.rn-lost-body` are `repeat(auto-fill,minmax(252px|236px,1fr))` grids; `.rem-row` and `.rn-row` are card grids (`grid-template-areas`), the flag dots are moved into the action row by `remRowEl` (`prepend`), banners/month heads tightened. Renewals: past months with nothing due fold into one line even when something was renewed in them («€1,600 renewed in these months»). Demo remaining page 4297px → ~3900px at 1440 with 3 columns instead of 13 rows.
  - *Quick Log:* per day, Booked is always shown in full; SMS and No show at most `max(2, booked that day)` entries and a «+ N more» button (`qlOpen` set, per session) opens the rest of that column (demo page 14,416px → 6,178px). *Callbacks:* one line per person (note ellipsised, phone, waiting chip on the same line — `.w-note`), the action buttons float over the row on hover (`.cb-acts` absolute) instead of squeezing the text; banner tighter. Board cards 8/10px padding; Year grid minmax 280px.
  - *Home:* the hero is one slim bar (greeting + date left, the Calls and Attention numbers as inline pills right; 2×2 grid on phones), and the cards sit in two columns where two lists pair well (Do ASAP | Booked meetings · Client reminders | Client chats · Follow up | Subscriptions; People to contact and Cash forecast stay full width). Demo Home 3,3xx px → 2,542 px at 1440.
  - *Home packer:* the two `.home-pair` groups are dealt into two `.hp-col` columns by height (`homePack()`, greedy: each card goes under the shorter column, original order kept per column; a ResizeObserver re-packs when the reminders/chats cards load; one column ≤1000px) so a tall card never leaves a hole next to it.
  - Also fixed: the «Changes done» quick reply asked for «2–3 επιλογές» of domain — now one name (Angelo's 21 Sept rule).
- **23 Sept 2026 (later) — Chats v7.1 (branch `chats-v7`, preview).** (1) *Board → chat backfill:* `chAutoCreate` is async and sequential and also covers every card with `month >= CH_AUTO_FROM` ('2026-09') in Need info / Building / Draft·Review / Making changes; chats are created EMPTY (`welcome:[]` — no e-mail can go to the client). (2) *Start bar* in an empty chat (`.ch-start`, `chStart`): «📷 Υλικό» / «🩺 Υλικό · doctoranytime» at stage yliko (sets `dat`), «👋 Send the welcome» otherwise; sends the messages, then `chShowLink` (link + Viber text). (3) *«Πάμε live» → Do ASAP:* the chat route writes `{uid:'live-<slug>-<msgid>', text:'Αγόρασε το domain X για Y στο Papaki — μετά «🌐 Domain connected»…', hot:true}` to `asap/inbox.json`; `/api/reminders` merges it into `asap`; the CRM re-reads reminders 4 s after a client message. (4) *Demo:* no DEMO_NOTE message, no yellow band, no automatic «domain connected» (Angelo presses the real button), and questions off the path (`isQuestion`) get NO automatic answer — the agent drafts (`chat.py list` shows «WAITING 🧪 DEMO»), and the notifier's approval ping now includes demo chats. (5) *Client page:* confirm/«πάμε live» sheets close the moment the client taps (`closeSheets()` before the send). (6) *Texts:* short Υλικό / Υλικό·doctoranytime messages (CRM, lib/chatdemo.js, `.advon/chat.py`), no e-mail invitation anywhere on the client page or in the Υλικό reminder e-mail (the header E-mail button stays), «κανονικά» removed from the three First draft messages.
- **22 Sept 2026 — CRM: Google on every lead, «Total profit», callbacks no-answer + priority, stats paused, Board ⇄ Chats both ways, paid from the chat (LIVE 22 Sept 2026 — Angelo said «put it live»; preview = demo copy published as a private claude.ai artifact, because Netlify builds no branch deploy for new branches — `crm-preview` returned «Site not found»).** Backups `index.backup-20260922.html` in crm and crm-demo.
  - *Leads:* the Google + Maps links show on every lead (row and call mode), not only on the «look up first» lists.
  - *Year → Net profit card:* a dark «Total profit» box under Net profit = `state.meta.wealth.base` (46,000) + (`realProfitToDate()` − `wealth.anchor`). `realProfitToDate()` = Σ over every month up to today of `earnedForMonth().total − expensesForMonth()` (only money actually received — paid cards + deposits, renewals, ticked monthly — minus expenses; uncollected «generated» never counts). The anchor is taken once, at the first boot of this version (`migrate20260922`). Click the box to change the starting amount.
  - *Statistics paused:* `state.meta.statsPaused` (set once by `migrate20260922`, real CRM only) makes `statsSince()` return `9999-12-31`, so every call figure (Board / Calendar / Quick Log month stats, Year totals, Home Calls, report) is 0 until Angelo presses the green **«Start counting from today»** (`[data-stats-start]` → `statsStartUI()` sets `statsSince=today`, un-pauses). Afterwards the same spot shows «↻ Restart from today». Export → Restart call statistics also un-pauses.
  - *Callbacks:* every row has **No answer** (`cbNoAnswer` → `item.tries[]` of dates, shown «no answer 22/09, 24/09 · 2 tries», Undo in the toast; tried-today rows sink below the others) and **Priority** (`item.pr` high/medium/low, chip before the name, click cycles; `cbSort` puts High first in every group). `pr` and `tries` travel with Later/Now moves.
  - *Board ⇄ Chats (two-way):* `CH2BOARD={yliko:'needinfo', draft:'building', changes:'review'}`, `BOARD2CH={needinfo:'yliko', review:'changes', changes:'changes'}` — Building does NOT move the chat (Angelo: the client's chat switches to First draft only when the draft is actually sent); sending a message containing «ολοκληρώσουμε την κατασκευή» (the First draft templates) while the chat is on Υλικό moves it to First draft by itself (in `Chats.send`). `c.chatStage` / `c.chatBoard` remember both sides at the last sync; whichever side changed since wins (chat moved → card moves; card dragged / edited → `Chats.meta(stage)`, `chPendStage` guards the race). Booked / Waiting payment / Paid / Thinking / Lost never move the chat and are never overwritten by it. First link = forward-only like before.
  - *Paid from the chat:* thread head shows «½ Half paid» (`payHalf`) and «€ Paid €X» / «€ Paid the rest €X» (`markPaidFromRemaining`) for the linked Board card → the money lands in In the bank + New money; once paid a green «✓ Paid €X» chip; chat list rows show «✓ paid» / «½ paid».
  - *Board:* cards in Paid are fully green (`.card.is-paid`, dark-mode variant).
